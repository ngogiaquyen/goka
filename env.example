## Database (Prisma)
# Format: mysql://USER:PASSWORD@HOST:PORT/DBNAME
# IMPORTANT: replace USER/PASSWORD with your real MySQL credentials.
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/goka"

## NextAuth OAuth providers (optional)
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
NEXT_PUBLIC_FACEBOOK_APP_ID=""
NEXT_PUBLIC_SHARE_URL="http://localhost:3000"

## Auth.js / NextAuth (required)
# Generate a long random string. Example (PowerShell):
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""
# Optional but recommended in production (Auth.js):
# AUTH_URL="http://localhost:3000"

## OTP Email (Resend) (optional)
# If RESEND_API_KEY is empty, app will log OTP to console in dev.
RESEND_API_KEY=""
RESEND_FROM="otp@goka.dev"

## OTP Email (SMTP) (optional)
# If these are set, app will send OTP via SMTP instead of Resend.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""

